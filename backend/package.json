{
  "name": "backend-api",
  "version": "1.0.0",
  "description": "Mock API para Sistema Angular",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.0"
  }
}
